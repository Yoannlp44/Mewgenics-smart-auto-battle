// Necromancer abilities AI patches
// Flatline: the caster safely kills themselves to spawn a shade clone -- never do this in auto-battle
Flatline.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// Flatline2: variant of Flatline, same issue
Flatline2.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// BloodGeyser: deals damage but self-damages for 50% max HP -- very risky
BloodGeyser.merge {
    damage_instance {
        ai_base_score -20
    }
}

// BloodGeyser2: variant of BloodGeyser, same self-damage risk
BloodGeyser2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// GigaDrain: melee attack, drains ALL current mana for damage -- slight penalty to avoid wasting full mana for weak hits
GigaDrain.merge {
    damage_instance {
        ai_base_score -5
    }
}

// GigaDrain2: variant of GigaDrain, same concern
GigaDrain2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Pestilence: aoe_mode all without restriction -- hits allies too damage 1, but still
Pestilence.merge {
    damage_instance {
        ai_base_score -50
    }
}

// Pestilence2: variant of Pestilence, same issue
Pestilence2.merge {
    damage_instance {
        ai_base_score -50
    }
}

// SlitWrists: damage 2 type melee on self -- bonus ability that the AI should never use actively
SlitWrists.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// Seppuku: Die 1 -- the caster kills themselves, never use in auto-battle
Seppuku.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// AbsorbSoul: SafeDie 1 on an ally -- kills an ally to gain power
AbsorbSoul.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// AbsorbSoul2: variant of AbsorbSoul, same issue
AbsorbSoul2.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// DemonicPact: RandomInjury 1 + TakeExtraTurn -- self-injury as cost
DemonicPact.merge {
    damage_instance {
        ai_base_score -20
    }
}

// DemonicPact2: variant of DemonicPact, same concern
DemonicPact2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// SoulTransfer: self_damage 4 + ConstitutionUp -1 -- hurts self and reduces stats
SoulTransfer.merge {
    damage_instance {
        ai_base_score -10
    }
}

// SoulTransfer2: variant of SoulTransfer, same concern
SoulTransfer2.merge {
    damage_instance {
        ai_base_score -10
    }
}

// DonateBlood: self_damage 10 to heal ally for 5 -- bad trade, net loss of HP
DonateBlood.merge {
    damage_instance {
        ai_base_score -10
    }
}

// DonateBlood2: variant of DonateBlood, same concern
DonateBlood2.merge {
    damage_instance {
        ai_base_score -10
    }
}

// TradeLife: swaps HP with an ally -- can leave caster dangerously low
TradeLife.merge {
    damage_instance {
        ai_base_score -5
    }
}

// TradeLife2: variant of TradeLife, same concern
TradeLife2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// LeechSwarm: aoe_mode all without exclude_allies -- applies Leeches to allies too
LeechSwarm.merge {
    damage_instance {
        ai_base_score -5
    }
}

// LeechSwarm2: variant of LeechSwarm, same concern
LeechSwarm2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// ForbiddenFamine: ApplyToRandomPartyMemberIfPossible GainDisorderFromPool -- disorder on a random ally
ForbiddenFamine.merge {
    damage_instance {
        ai_base_score -20
    }
}

// ForbiddenFamine2: variant of ForbiddenFamine, same concern
ForbiddenFamine2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Hush: applies Sleep 1 without restriction -- can put allies to sleep max_range 1-4
Hush.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Hush2: variant of Hush, same concern
Hush2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Shriek: aoe_mode cone without aoe_restrictions -- Confusion + Madness can hit adjacent allies
Shriek.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Shriek2: variant of Shriek, same concern
Shriek2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// BloodRain: allies_only heal but self_damage 1 -- slight penalty for self-harm
BloodRain.merge {
    damage_instance {
        ai_base_score -5
    }
}

// WeAreOne: SoulLink on all enemies + self -- links own HP to enemies
WeAreOne.merge {
    damage_instance {
        ai_base_score -5
    }
}
