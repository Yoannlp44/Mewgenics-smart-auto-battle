// Butcher abilities AI patches
// SelfMutilate: deals self-damage generic_physical to gain StrengthUp+Cleave -- slight penalty, part of the Butcher kit
SelfMutilate.merge {
    damage_instance {
        ai_base_score -5
    }
}

// SelfMutilate2: variant of SelfMutilate, same concern
SelfMutilate2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Fartoom: AoE around self, deals 25% current HP as damage BUT also self-damages 25% current HP
Fartoom.merge {
    damage_instance {
        ai_base_score -10
    }
}

// Fartoom2: variant of Fartoom, same self-damage concern
Fartoom2.merge {
    damage_instance {
        ai_base_score -10
    }
}

// LightenTheLoad: costs 25 HP type spell_cost to gain TakeExtraTurn -- risky when low HP
LightenTheLoad.merge {
    damage_instance {
        ai_base_score -10
    }
}

// LightenTheLoad2: variant of LightenTheLoad, same concern
LightenTheLoad2.merge {
    damage_instance {
        ai_base_score -10
    }
}

// SkullBash: SelfStun 1 guaranteed v1 -- stuns self after attacking
SkullBash.merge {
    damage_instance {
        ai_base_score -20
    }
}

// SkullBash2: SelfStun 1 at 50% chance v2 -- less risky but still penalize
SkullBash2.merge {
    damage_instance {
        ai_base_score -10
    }
}

// Consume: self_damage Drowsy 1 -- puts self to sleep while collecting pickups
Consume.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Consume2: variant of Consume, same concern
Consume2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Binge: SpeedUp -1 on self -- reduces own speed
Binge.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Binge2: variant of Binge, same concern
Binge2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// TaintedOffering v1: self_damage Sleep 1 -- puts self to sleep to activate MeleeRevengeDamage
TaintedOffering.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// TaintedOffering2: no self-Sleep, just MeleeRevengeDamage passive -- slight situational penalty
TaintedOffering2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Tromp v1: ApplyToSource Bruise 1 -- self-injures on cast
Tromp.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Chonkwalk: damage 5 type spell_cost -- hurts self to refresh movement
Chonkwalk.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Chonkwalk2: variant of Chonkwalk, same concern
Chonkwalk2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Reflux: self_damage Burn 2 -- sets self on fire
Reflux.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Reflux2: variant of Reflux, same concern
Reflux2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Tryptophan v1: AoE Sleep 1 without restriction, max_aoe 1 -- can put adjacent allies to sleep
Tryptophan.merge {
    damage_instance {
        ai_base_score -99999
    }
}

// Tryptophan2: Sleep AoE + heals self -- sleep still hits allies, heavy penalty
Tryptophan2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Butcher: aoe_mode all -- instakills anything with <=3 HP, can kill weakened allies
Butcher.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Butcher2: variant of Butcher, same concern
Butcher2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Cough: self_damage 2 to spawn RotFly -- slight penalty for self-harm
Cough.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Cough2: variant of Cough, same concern
Cough2.merge {
    damage_instance {
        ai_base_score -5
    }
}

// MyTurn: StealTurn 1 from an ally must_have_ally must_have_player_cat -- steals ally's turn
MyTurn.merge {
    damage_instance {
        ai_base_score -20
    }
}

// MyTurn2: variant of MyTurn, same concern
MyTurn2.merge {
    damage_instance {
        ai_base_score -20
    }
}

// Track: ForceMoveTowardsTaggedObject tag food -- wastes action if no food on map
Track.merge {
    damage_instance {
        ai_base_score -5
    }
}

// Track2: variant of Track, same concern
Track2.merge {
    damage_instance {
        ai_base_score -5
    }
}
